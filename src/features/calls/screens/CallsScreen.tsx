import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export function CallsScreen() {
  return (
    <ScreenPlaceholder
      description="Historial de llamadas, signaling y estados de llamada se conectaran desde este modulo."
      title="Llamadas"
    />
  );
}
